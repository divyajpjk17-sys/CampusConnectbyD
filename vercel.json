{
  "version": 2,
  "builds": [
    {
      "src": "campusconnect-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/campusconnect-frontend/$1"
    }
  ]
}
